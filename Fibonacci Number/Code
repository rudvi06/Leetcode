class Solution {
    public int fib(int n) {
        int first=0,second=1,curr=0;
        for(int i=2;i<=n+1;i++){
            curr=first+second;
            second=first;
            first=curr;
        }
        return curr;
    }
}
