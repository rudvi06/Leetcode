class Solution {
    public boolean isPowerOfFour(int n) {
        
        boolean bool=false;
        
        if(n==0)
            return false;
        
        if(n==1)
           return true;
        
        if(n%4==0)
            bool=isPowerOfFour(n/4);
        
        return bool;
    }
    
}
